<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">

    <persistence-unit name="aureus" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- ENTITY -->
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaUser</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaUserDetail</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaCollection</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaCoin</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaTransaction</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaCoinTransaction</class>

        <properties>
            <!-- JDBC connection properties -->
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/AUREUS?useSSL=false&amp;serverTimezone=UTC"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value="rootpassword"/>

            <!-- Hibernate properties -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="validate"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>

    <!-- Persistence unit for tests -->
    <persistence-unit name="aureus_test" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaUser</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaUserDetail</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaCollection</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaCoin</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaTransaction</class>
        <class>cat.uvic.teknos.dam.aureus.model.jpa.JpaCoinTransaction</class>

        <properties>
            <!-- JDBC connection properties for tests -->
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/AUREUS_TEST?useSSL=false&amp;serverTimezone=UTC"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value="rootpassword"/>

            <!-- Hibernate properties for tests -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

        </properties>
    </persistence-unit>
</persistence>